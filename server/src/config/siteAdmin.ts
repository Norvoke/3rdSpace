// Site-owner override: this account can delete any post, comment, or group
// as if they'd created it themselves, same as the auto-friend-on-signup rule
// in config/passport.ts. Not user-configurable by design — a single hardcoded
// account, not a role system.
export const SITE_ADMIN_USERNAME = 'finnellingwood';
